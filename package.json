{
  "name": "error-fix-demo-app",
  "version": "1.0.0",
  "description": "Simple Node.js API with an intentional runtime error for stack trace demo",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
